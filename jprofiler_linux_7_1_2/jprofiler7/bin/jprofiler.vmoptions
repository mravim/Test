# For 32-bit JVMs, raise the -Xmx value carefully because otherwise snapshot
# files cannot be memory mapped anymore and all operations slow down.
-Xmx768m
-Xss2m
#
# uncomment to get a high contrast theme for the thread history view
#-Djprofiler.highContrastMode=true
#
# uncomment to override the CSV separator
#-Djprofiler.csvSeparator=,
